#include <stdio.h>

int main() {
    // Define two integers
    int num1 = 5;
    int num2 = 10;

    // Calculate the sum of the two numbers
    int sum = num1 + num2;

    // Display the sum
    printf("The sum of %d and %d is %d\n", num1, num2, sum);

    return 0;
}